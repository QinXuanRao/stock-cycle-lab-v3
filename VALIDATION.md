# 验证范围与已知边界

## 已验证

- Windows 本机 Node.js v24.18.0：41项自动化测试通过。
- 原始财务数据推导单季增长/EPS TTM；公告时点、财年标识、重述分母、负/零基数及资本口径检查。
- CN/US独立参数，缺美元门槛不放行；US不套用CN ST/日涨跌停规则。
- EMA、Wilder ATR、六类状态优先级、前置风险状态延续、无未来数据重标、震荡/下跌过滤。
- 重复/缺失/滞后/非法OHLC、日历、复权及股票池覆盖不足阻断。
- JSON与六文件CSV路线得到完全一致的数值/结果；CSV公式注入防护、配置溯源及输入指纹。
- 不带Node模块接口的浏览器式JavaScript隔离环境，使用Web Crypto计算，与Node引擎输出一致。
- 前端脚本语法、DOM标识对应关系、禁用网络的内容安全策略静态检查。
- 命令行可输出JSON/CSV/清单，拒绝覆盖已存在运行目录。

## 规模试验

6000只合成股票，每只220根日线；本机命令行一次运行约10.8秒，峰值RSS约1134MB（约1.1GB）。这是能力/内存试验，不是真实全市场扫描，不代表任何策略收益；速度取决于硬件、输入长度和输出大小。浏览器内存不足时可分批或使用CLI。

## 尚未验证

- 当前内置浏览器安全策略禁止自动打开本地file页面，因此没有完成真实浏览器UI端到端测试。没有绕过该限制。
- Web Crypto隔离环境一致性及静态前端检查不能替代真实浏览器交互测试。
- 尚未在macOS/Linux、Safari、Firefox上实机验证。实现使用标准浏览器API、无硬编码电脑路径，具备可移植设计，但不保证所有设备版本都能运行。
- 未接入/验证真实全市场数据商、未开展收益回测、交易成本或实际成交验证。

## 用户本机自检

1. 用现代浏览器打开Stock-Cycle-Lab.html。
2. 点击「加载合成演示」「运行评估」：预期显示2只输入、2只基本面通过、2只双门槛通过、2只完整资格，且全程标明合成演示。
3. 恢复默认参数后再运行：美股美元门槛为空，预期只有A股合成样例保留完整资格，美股显示证据不足。
4. 搜索/切换三层名单、查看证据与运行清单、导出CSV/JSON。
5. 使用自己的真实数据前，核对来源、币种、当期日历、财报组件和美股阈值。

以上自检是软件行为验证，不是策略投资价值验证。
